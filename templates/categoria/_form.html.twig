{% import 'macro/action.html.twig' as action %}

{{ action.frm_start(form) }}
    
    {{ action.frm_column(
        action.frm_column_field(form.nombre,null,null,2),
        null,null,null,1
    ) }}
    {{ action.frm_column(
        action.frm_column_field(form.detalle,null,null,2),
        null,null,null,1
    ) }}
    {{ action.frm_column(
        action.frm_column_btn(button_label|default('Guardar')),
        null,
        null,
        null,
        2
    ) }}
{{ action.frm_end(form) }}