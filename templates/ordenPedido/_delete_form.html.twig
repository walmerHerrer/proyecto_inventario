<form method="post" action="{{ path('ordenPedido_delete', {'id': ordenPedido.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordenPedido.id) }}">
    <button class="btn">Delete</button>
</form>
